import type { DefaultThemeLocaleOptions } from '../../shared';
/**
 * Assign default options to `themeConfig`
 */
export declare const assignDefaultLocaleOptions: (localeOptions: DefaultThemeLocaleOptions) => void;

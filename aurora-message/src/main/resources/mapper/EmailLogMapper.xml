<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xyz.xcye.message.dao.EmailLogDao">


    <insert id="insertEmailLog" keyProperty="uid" keyColumn="uid" useGeneratedKeys="true">
        insert  into au_email_log
        (sender,subject,content,receiver,create_time,is_send)
        values (
        #{emailLog.sender},#{emailLog.subject},#{emailLog.content},#{emailLog.receiver},#{emailLog.createTime},#{emailLog.send}
        )
    </insert>

    <update id="updateEmailLog">
        update au_email_log
        <set>
            <if test="emailLog.sender != null">
                sender=#{emailLog.sender},
            </if>
            <if test="emailLog.subject != null">
                subject=#{emailLog.subject},
            </if>
            <if test="emailLog.content != null">
                content=#{emailLog.content},
            </if>
            <if test="emailLog.receiver != null">
                receiver=#{emailLog.receiver},
            </if>
            <if test="emailLog.updateTime != null">
                update_time=#{emailLog.updateTime},
            </if>
            <if test="emailLog.send != null">
                is_send=#{emailLog.send},
            </if>
        </set>
        where uid=#{emailLog.uid}
    </update>

    <delete id="deleteEmailLog">
        delete from au_email_log where uid=#{uid}
    </delete>

    <select id="queryAll" resultMap="emailLogMap">
        select uid,sender,subject,content,receiver,create_time,update_time,is_send from au_email_log
        <where>
            <if test="emailLog.sender != null">
                sender like contact('%',#{emailLog.sender} ,'%')
            </if>
            <if test="emailLog.subject != null">
                and subject like contact('%',#{emailLog.subject},'%')
            </if>
            <if test="emailLog.content != null">
                and content like contact('%',#{emailLog.content},'%')
            </if>
            <if test="emailLog.receiver != null">
                and receiver like contact('%',#{emailLog.receiver},'%')
            </if>
            <if test="emailLog.createdAt != null">
                and create_time like contact('%',#{emailLog.createTime},'%')
            </if>
            <if test="emailLog.updateTime != null">
                and update_time like contact('%',#{emailLog.updateTime},'%')
            </if>
            <if test="emailLog.send != null">
                and is_send like contact('%',#{emailLog.send},'%')
            </if>

        </where>
    </select>

    <select id="queryByUid" resultMap="emailLogMap">
        selectuid,sender,subject,content,receiver,create_time,update_time,is_send from au_email_log where uid=#{uid}
    </select>

    <resultMap id="emailLogMap" type="xyz.xcye.common.dos.EmailLogDO">
        <id column="uid" property="uid"/>
        <result column="sender" property="sender"/>
        <result column="subject" property="subject"/>
        <result column="content" property="content"/>
        <result column="receiver" property="receiver"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="send" property="sender"/>
    </resultMap>
</mapper>